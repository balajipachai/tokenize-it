node_modules/
vendor/
.env
.env.local
dist/
build/
artifacts/
cache/
typechain-types/
coverage/
*.log
.DS_Store

# editor swap files
*.swp

# Privy payroll officer signing keys — never commit
.env.payroll.local

# indexer output — derived state, rebuildable from chain
services/indexer/cap-table.json
